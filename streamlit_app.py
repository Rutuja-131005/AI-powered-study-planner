import sys
import os

# Add the AI Study Planner directory to the Python path
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'AI Study Planner'))

# Import and run the app
from app import *
